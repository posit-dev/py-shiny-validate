{
  "name": "shiny-validate-pkg-template",
  "version": "1.0.0",
  "description": "Demo of custom JS components for Shiny for python",
  "main": "index.js",
  "scripts": {
    "build": "esbuild srcts/shinyvalidate.ts --bundle --outfile=shiny_validate/distjs/index.js",
    "watch": "npm run build -- --watch"
  },
  "author": "Nick Strayer",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.19.5",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@posit-dev/shiny-bindings-core": "^0.0.2",
    "lit": "^3.0.2"
  }
}
